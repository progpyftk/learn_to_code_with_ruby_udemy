# the ternary operator is used to replace if else structures - sugar sintax
# without ternary operator
if 1 < 2
    puts "Yes, it is!"
else
    puts "No, it is not"
end

# using ternary conditio ? true : false
puts 1 < 2 ? "Yes, it is" : "No, it is not"
puts 1.even? ? "It is even" : "Is is odd"